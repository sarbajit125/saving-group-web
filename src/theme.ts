import { createTheme } from '@mantine/core';

export const theme = createTheme({
  primaryColor: 'indigo',
  primaryShade: 6,
  fontFamily: 'Roboto, Lato',
  headings: {
    fontFamily: 'Roboto',
  },
});
